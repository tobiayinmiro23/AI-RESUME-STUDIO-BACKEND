
export interface signInType { message: {
                email: string,
                id: string,
                token: string,
            }, success: boolean };

export interface signUpType { message: string, success: boolean };